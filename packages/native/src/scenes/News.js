import React from 'react'
import PropTypes from 'prop-types'
import {View, Text} from 'react-native'

const News = (props) => {

  return (
    <View><Text>me is native news jk</Text></View>
  )
};

News.propTypes = {
  prop: PropTypes.string
};

export default News
